package org.fundacionparaguaya.advisorapp.models;

/**
 * The type of response that a question should have.
 */

public enum ResponseType {
    INTEGER,
    STRING,
    INDICATOR,
    PHONE_NUMBER,
    PHOTO,
    LOCATION
}